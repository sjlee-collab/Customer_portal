# 고객지원포탈 보안 문서

빅스데이터 고객지원포탈(`support.bigxdata.io`)의 보안 설계·적용 조치·운영 절차를 한곳에 모은 문서.
최종 갱신 **2026-09-22**.

구현 세부는 [CLAUDE.md](CLAUDE.md), 변경 이력은 [CHANGELOG.md](CHANGELOG.md) 참고.

---

## 1. 시스템 개요 (보안 관점)

| 계층 | 구성 | 보안 요점 |
|---|---|---|
| 프런트 | AWS Amplify 호스팅, 단일 파일 SPA(`index.html`) | 응답 헤더 8종 적용(`customHttp.yml`). 인라인 스크립트 구조라 CSP `unsafe-inline` 불가피 → XSS 방어는 출력 이스케이프로 담당 |
| 진입 | HTTP API Gateway + Lambda Authorizer | 모든 인증 라우트가 `jwt-authorizer`를 거침. 라우트 미등록 시 404(코드만으론 노출 안 됨) |
| 처리 | Lambda 7개 (api-layer, data-api, storage-api, public-inquiry, jwt-authorizer, send-email, notify-handler) | 비밀값은 전부 Secrets Manager. 응답에 `no-store`·`nosniff` |
| 데이터 | RDS PostgreSQL 18 (VPC 사설 서브넷) | 공개 접근 불가, 저장 암호화, 자동 백업 7일 + 시점 복구, 삭제 방지 ON. 연결 시 서버 인증서 검증 |
| 파일 | S3 버킷 3개 | 공개 차단 4종(계정·버킷), AES256, 버저닝 + 이전 버전 90일 만료. 접근은 presigned URL만(최대 300초) |
| 외부 통신 | NAT 인스턴스 1대 | VPC Lambda의 유일한 아웃바운드 경로. 루트 볼륨 암호화 |

**고객망 대응**: 일부 고객사가 `*.amazonaws.com`을 차단해 Amplify 리라이트로 같은 출처 경유 경로를 제공한다(`/api/*`, `/files/ticket-attachments/*`).

---

## 2. 인증·인가

### 2.1 비밀번호

- **scrypt만 인정.** `scrypt$salt$hash` 형식이 아니면 즉시 거부(2026-09-18).
  레거시 SHA-256·평문 비교 분기는 제거됨 — DB에 다른 형식을 심어도 로그인 불가, 재설정으로만 복구.
- **로그인 잠금**: 연속 5회 실패 → 15분 잠금. 잠금 중에는 정답도 거부하고 카운터를 올리지 않는다(잠금 연장 방지).
- **재설정 요청 쿨다운**: 같은 주소 15분(메일 폭탄 방지).

### 2.2 로그인 응답 균일화

거부 사유(미등록·비활성·비밀번호 미설정·잠금·비밀번호 오류)와 무관하게 **항상 동일한 401과 동일 문구**.
미등록 경로에도 더미 해시 검증을 1회 태워 **응답 시간까지 맞춘다**(시간차로 계정 존재를 재는 것을 막기 위함).
실제 사유는 CloudWatch 로그에만 남는다.

### 2.3 토큰

- JWT, 유효기간 8시간, 브라우저 `localStorage` 보관.
- **폐기 수단**: `users.token_version`을 토큰 `ver` 클레임과 요청마다 대조.
  다음 경우 버전이 올라가 기존 토큰이 즉시 무효 — 비밀번호 변경·재설정·관리자 재설정·로그아웃·권한(역할/회사/계약/조직/활성) 변경.
- 서명 키는 `api-layer`와 `jwt-authorizer`가 **같은 시크릿**을 본다. 키를 바꾸면 발급된 토큰이 전부 무효가 된다.

### 2.4 테넌트 격리

- 읽기: 역할별 필터가 회사·계약·조직 범위를 강제.
- 쓰기: `PATCH`/`DELETE`에도 테넌트 조건을 SQL에 직접 결합. 비스태프는 `role`·`company_id` 등 권한 상승 컬럼을 바꿀 수 없다.
- 민감 컬럼(`password`, `reset_token`, `token_version` 등)은 조회·쓰기·**필터·정렬**에서 모두 차단(필터로 값을 한 글자씩 추출하는 경로까지 봉쇄).

---

## 3. 적용된 보안 조치

### 3.1 1차 하드닝 (2026-08)

비밀번호 해시 SHA-256 → scrypt 전환(로그인 시 자동 승격), 기본 인가 정비.

### 3.2 2차 하드닝 (2026-09-15 ~ 18)

| 항목 | 내용 |
|---|---|
| 저장형 XSS | 출력 이스케이프가 따옴표를 처리하지 않아 속성값 삽입이 가능했음 → 수정 |
| 응답 헤더 | Lambda 응답에 `Cache-Control: no-store, private`, `X-Content-Type-Options: nosniff` |
| presigned URL | 다운로드 만료를 클라이언트가 무제한 지정할 수 있었음 → 최대 300초로 제한 + `no-store` |
| 쓰기 테넌트 가드 | `PATCH`/`DELETE`에 테넌트 조건 결합, 권한 상승 컬럼 차단 |
| 로그인 잠금 | 5회/15분 + 재설정 쿨다운 |
| 필터 오라클 | 차단 컬럼을 필터·정렬에 쓰지 못하게 |
| 토큰 폐기 | `token_version` 도입 |
| 고객망 우회 | 같은 출처 경유 경로(API·첨부) |

### 3.3 계정 존재 노출 차단 (2026-09-18)

로그인 폼·계정 신청 폼으로 **임의 이메일의 가입 여부를 알 수 있던 경로**를 닫았다.

- 계정 신청 시 이미 가입된 주소면 예전에는 오류로 알려줬으나, 지금은 **새 신청과 똑같이 접수 응답**을 주고
  안내는 **그 주소의 우편함으로만** 보낸다(주소당 15분 쿨다운).
- 신청 폼의 이메일 사전 중복 확인 기능 제거.
- 로그인·재설정 응답 균일화(2.2).

### 3.4 레거시 비밀번호 정리 (2026-09-18)

- 구식 해시로 남아 있던 **내부 계정 52건**의 비밀번호를 무효화(고객 계정 0건). 모두 6주 이상 미로그인 상태여서 즉시 영향 없음.
- 이후 코드에서 레거시 비교 분기 제거(2.1).
- 복구는 로그인 화면의 "비밀번호를 잊으셨나요?"로 본인이 직접.

### 3.5 오픈소스 점검 후속 (2026-09-22)

도구 6종 점검 결과 실제 갭 10건을 조치. 상세는 5장.

| 코드 | 내용 |
|---|---|
| S-1 | RDS 연결 시 서버 인증서 검증(이전엔 암호화만 하고 상대를 확인하지 않음) |
| S-2 | 티켓 이력 상세값 이스케이프(도달 불가한 잠재 지점, 예방) |
| S-3 | 옛 Supabase 프로젝트 일시정지(git 이력의 공개키로 접근 가능하던 옛 데이터 차단) |
| S-4 | 오류 문구 표시 이스케이프 |
| S-5 | 2세대 보안 헤더 3종 추가 |
| P-1 | Lambda 비밀값 전량 Secrets Manager 이관, 환경변수에서 제거 |
| P-2 | S3 계정 레벨 공개 차단 |
| P-3 | S3 버저닝 + 이전 버전 90일 만료 |
| P-4 | API Gateway 액세스 로그(쿼리스트링 제외 포맷, 보존 90일) |
| P-5 | NAT 인스턴스 암호화 볼륨 무중단 교체 + 리전 기본 디스크 암호화 |

---

## 4. 비밀값 관리

**Lambda 환경변수에 비밀값은 하나도 없다**(7개 함수 전수 확인, 2026-09-22).

| 시크릿 | 담긴 값 | 사용 함수 |
|---|---|---|
| `customer-portal/jwt` | JWT 서명 키 | api-layer, jwt-authorizer (반드시 같은 값) |
| `customer-portal/ms-graph` | 메일 발송용 클라이언트 시크릿 | send-email |
| `customer-portal/slack-webhooks` | 슬랙 웹훅 5종 | notify-handler, public-inquiry |
| `rds!db-…` (AWS 관리) | DB 비밀번호 (7일 주기 자동 회전) | api-layer, data-api, public-inquiry |

**조회 방식**: `secrets.mjs` — 외부 라이브러리 없이 내장 암호화 기능으로 직접 서명해 시크릿 1건만 가져온다.
일부 함수는 런타임에 AWS SDK가 포함되지 않고 배포 도구가 소스 파일만 다루기 때문이다.
컨테이너당 1회 조회 후 캐시하므로 평상시 응답 지연은 없다.

**읽기 권한**: IAM 정책 `customer-portal-read-secrets`(접두 `customer-portal/*` 한정)를 실행 롤 5개에 부착.

### 교체 절차

1. Secrets Manager에서 **값만 수정**한다. Lambda 재배포는 필요 없다(다음 콜드스타트부터 반영).
2. 즉시 반영이 필요하면 해당 함수의 환경변수를 아무거나 하나 바꿔 새 컨테이너를 띄운다.
3. **JWT 키를 바꾸면 발급된 토큰이 전부 무효**가 되어 전 사용자가 다시 로그인해야 한다. 사용량 적은 시간에.

---

## 5. 보안 점검 이력

### 5.1 외부 AI 보고서 검증 (2026-09-15 ~ 18)

HAR 파일 기반 AI 취약점 보고서 2건(지적 12건)을 실제 코드·라이브 API로 검증.

- **실제 갭 5건** → 전부 조치(3.2)
- **오탐 6건** — 인증 없는 접근·헤더 전무·SQL 주입·CORS 과다 등은 실측 결과 문제 없음
- **절차 문제 1건** — HAR 파일에 비밀번호·토큰이 평문으로 담긴다. 아래 7.2 참고

### 5.2 오픈소스 도구 점검 (2026-09-18 ~ 22)

| 도구 | 점검 축 | 결과 |
|---|---|---|
| gitleaks | git 이력 930커밋 비밀값 | 3건(옛 공개키) → S-3 |
| Trivy | 배포본 의존성 CVE 144패키지 | **0건** |
| Semgrep + 자체 감사 스크립트 | 정적 코드, 화면 삽입 지점 300곳 | 4건 → S-1·S-2·S-4 |
| OWASP ZAP (수동) | 응답 헤더·정보 노출 | 8건, 실제 갭 0 |
| Nuclei | 알려진 취약점·노출 3,677템플릿 | 전부 정보성, 헤더 1건 → S-5 |
| Prowler | AWS 계정 설정 280체크 | Critical 실제 6건 등 → P-1~P-5 |

**전 축 Critical·High 0건.** 조치 후 같은 도구로 재점검해 해소를 확인했다.

> 스캐너 결과는 자동 판정이다. 이번에도 Critical 10건 중 4건이 오탐이었다(환경변수 참조 코드·라이브러리 타입 정의를 비밀값으로 오인). **반드시 실제 코드·설정값을 확인해 걸러야 한다.**

### 5.3 정기 점검 권장

| 주기 | 도구 | 이유 |
|---|---|---|
| 월 1회 또는 배포 시 | Trivy | 의존성 취약점은 시간이 지나며 새로 발견됨 |
| 주 1회 또는 커밋 훅 | gitleaks | 새 비밀값이 이력에 들어가는 것을 조기 차단 |
| 프런트 대규모 수정 시 | Semgrep + 감사 스크립트 | 단일 파일 구조상 이스케이프 누락 재발 가능 |
| 분기 1회 | Nuclei, Prowler | 인프라 설정 변경·새 취약점 지문 반영 |

---

## 6. 구조적 수용 — 고치지 않기로 판단한 것

| 지적 | 등급 | 이유 |
|---|---|---|
| CSP `script-src`/`style-src` `unsafe-inline` | Medium | 단일 파일 SPA라 인라인 스크립트와 `onclick` 속성이 구조 자체. 제거하면 화면이 동작하지 않는다. 프런트를 파일 분리 구조로 재작업할 때 함께 해결될 항목. **방어는 출력 이스케이프 전수 적용으로 대체** |
| `Server: AmazonS3` 헤더 | Low | Amplify 호스팅이 붙이는 고정 헤더, 제거 수단 없음. 버전 정보가 아니라 정보 가치도 없음 |
| API Gateway 루트 404 응답의 헤더 누락 | Info | HTTP API는 게이트웨이 자체 응답의 헤더를 바꿀 수 없다. 실제 함수 응답에는 헤더가 붙는다 |
| `Cross-Origin-Embedder-Policy` 누락 | Info | 켜면 S3 첨부 이미지가 전부 깨진다. 이 포탈에서 방어 이득이 실질적으로 없어 의도적 제외 |

---

## 7. 잔여 과제

### 7.1 결정 대기

| 항목 | 내용 | 필요한 결정 |
|---|---|---|
| 공용 초기 비밀번호 | 2026-08-12 고객 계정 236건을 동일한 알려진 값으로 일괄 등록. 그중 다수가 아직 그대로일 가능성 | (a) 첫 로그인 시 변경 강제 플래그 도입 (b) 일괄 무효화 + 재설정 안내. **(a) 권장** — 고객 불편이 적고 이후 일괄 등록에도 재사용 가능 |
| 미로그인 내부 계정 34건 | 비밀번호 무효화로 사실상 잠긴 상태 | 비활성화할지 그대로 둘지 |
| 옛 Supabase 프로젝트 | 일시정지 상태(접근 차단됨) | 남은 데이터 확인 후 완전 삭제 여부 |
| 옛 NAT 인스턴스 | 중지 상태로 롤백 경로 보존 | 2026-09-29 이후 종료 |

### 7.2 절차 (코드 아님)

- **HAR 파일 취급**: 지원 과정에서 HAR을 요청하지 않는다. HAR에는 **비밀번호 원문·토큰·조회한 데이터가 평문으로** 담긴다.
  오류 확인은 브라우저 콘솔 화면 캡처로 충분하다. 부득이 받았다면 분석 후 즉시 삭제하고, 제공자에게 **비밀번호 변경과 로그아웃 1회**를 요청한다(로그아웃하면 토큰이 즉시 무효화된다).
- **이미 제공된 HAR** 관련 담당자 비밀번호 변경 안내 필요.
- **레거시 비밀번호 정리 사내 공지** — 옛 비밀번호가 무효화됐고 "비밀번호를 잊으셨나요?"로 재설정하면 된다는 안내.

### 7.3 미실행 점검

**ZAP 능동 스캔** — 파라미터 주입·인가 우회를 실제 요청으로 탐지하는 방식. 6종 중 유일하게 **쓰기가 발생**한다.

개발 환경과 운영 환경이 백엔드를 공유하므로 그대로 돌리면 운영 데이터베이스에 쓰레기 데이터가 쌓이고, 실제 슬랙·메일 알림이 대량 발송되며, 로그인 시도로 실사용자 계정이 잠긴다.

실행하려면 다음을 먼저 갖춰야 한다.

1. 알림 우회(테스트 채널·테스트 주소로 전환)
2. 전용 테스트 계정·회사로 범위 한정
3. 로그인·재설정 라우트 제외
4. 요청 속도 제한
5. 사후 데이터 정리

**기대 수확은 낮다.** 주요 대상인 SQL 주입과 저장형 XSS는 이미 코드 점검으로 확인했다. 남는 것은 개별 라우트의 입력 검증 누락 정도이며, 동적 스캔은 오탐 비율이 가장 높아 검토 부담이 크다.

**하는 게 의미 있어지는 시점**: 개발용 백엔드를 분리한 뒤, 또는 외부 감사·인증 심사에서 동적 진단 증적을 요구할 때.

### 7.4 도입 보류

- **AWS WAF** — 비용 문제로 도입하지 않기로 결정. 대신 계정 잠금·응답 균일화로 대응 중.
- **IP 단위 속도 제한** — HTTP API는 IP별 제한을 지원하지 않는다. WAF 없이는 함수 내 카운터뿐인데, 회사 공용 IP 뒤 다수 사용자를 통째로 막는 부작용이 있어 보류.

---

## 8. 사고 대응 참고

| 상황 | 대응 |
|---|---|
| 토큰 유출 의심 | 해당 사용자 로그아웃 또는 비밀번호 변경 → 토큰 즉시 무효. 전체 무효화가 필요하면 JWT 시크릿 교체(전 사용자 재로그인) |
| 비밀값 유출 의심 | Secrets Manager에서 값 교체(재배포 불필요). 슬랙 웹훅은 슬랙에서 재발급 후 시크릿 갱신 |
| 첨부 실수 삭제 | 버저닝 적용됨 — **90일 이내**면 삭제 표시를 제거해 복구 가능 |
| DB 사고 | 자동 백업 7일 + 시점 복구. 삭제 방지 설정 ON |
| NAT 장애 | 중지된 옛 인스턴스를 시작하고 라우트를 되돌린다(절차는 CLAUDE.md) |
| 배포 후 이상 | Lambda는 직전 배포본 zip이 보관되어 있어 재배포로 즉시 롤백. 프런트는 되돌리기 커밋 후 푸시 |
| API 이상 접근 조사 | API Gateway 액세스 로그(보존 90일)에서 요청 경로·상태·IP·지연 확인. 개인정보 보호를 위해 쿼리스트링은 기록하지 않는다 |

---

## 9. 관련 문서

- [CLAUDE.md](CLAUDE.md) — 구현 세부, 배포 절차, 주의사항
- [CHANGELOG.md](CHANGELOG.md) — 변경 이력
- `scripts/harness/README.md` — 회귀 테스트 구성(보안 관련 테스트 포함: 인증, 토큰 폐기, 로그인 잠금, 계정 존재 노출 차단, 첨부 권한)
